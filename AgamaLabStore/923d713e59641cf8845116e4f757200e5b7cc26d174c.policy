permit(
  principal is Jans::Workload,
  action in [Jans::Action::"GET"],
  resource == Jans::HTTP_Request::"AcrsResource"
)
when {
  principal has access_token.scope &&
  principal.access_token.scope.contains("ACR_READ_ACCESS")
};